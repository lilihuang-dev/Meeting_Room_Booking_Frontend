// const convertDateTimeStrToBackendFormat = (datetime) => {
//     const date = new Date(datetime);
//     return date.toISOString().slice(0, 19).replace('T', ' ');
// };

// export default convertDateTimeStrToBackendFormat;
